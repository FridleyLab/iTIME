---
title: "Univariate_Report"
author: "iTIME Dev Team"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: pdf_document
params:
  selected_marker: NULL # = input$picked_marker
  contingency_threshold: NULL # = input$choose_cont_thresh
  picked_clinical: NULL # = input$picked_clinical
  boxplot: NULL # = boxplot()
  contingency_Table: NULL # = contTable()
  frequency_table: NULL # = freqTable()
  summary_table: NULL # = summaryTable()
  total_cell_column: NULL # = input$picked_total_cells
  modeling_reference: NULL # = input$picked_modeling_reference
  selected_univariate_model: NULL # = input$selectedModel
  chosen_model_stats: NULL # = model_stats()
  cdf_plot: NULL # = cdfplot()
  model_aic_table: NULL # = aic_table()
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
